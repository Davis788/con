# ⚠️ ВНИМАНИЕ: Это удалит ВСЁ, что мы устанавливали!
# Выполняйте только если уверены!

x-ui uninstall 2>/dev/null
rm -rf /usr/local/x-ui /usr/bin/x-ui /usr/local/etc/xray
rm -rf /opt/marzban /var/lib/marzban
docker compose -f /opt/marzban/docker-compose.yml down 2>/dev/null
wg-quick down wg0 2>/dev/null
rm -rf /etc/wireguard
rm -rf /opt/telegram-bot /opt/github-merger
rm -f /etc/systemd/system/{x-ui,telegram-bot,github-merger}.service
systemctl daemon-reload
apt autoremove -y
apt autoclean
echo "✅ Очистка завершена!"